<%= form_with model: [@blog, @comment] do |f|%>
  <div class="field">
    <%= f.label :comment, "コメント編集" %></br>
    <%= f.text_field :comment %>
  </div>

  <div class="submit">
    <%= f.submit "送信"%>
  </div>

<% end %>